[project]
name = "librarian"
version = "0.0.1.dev0"
description = "The Librarian Paradigm: An Agentic Solution for Structured Knowledge and Grounded Reasoning"
authors = []
requires-python = ">=3.10,<3.13"
dependencies = [
    "camel-ai[all]>=0.2.46"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["librarian"]

[tool.poetry]
name = "librarian"
version = "0.1.0"
description = "Dev repo for proof-of-concept for the knowledge & reasoning decoupling paradigm"
authors = []
packages = [{include = "librarian"}]

[tool.poetry.dependencies]
python = ">=3.10.0,<3.12"
camel-ai = {version = "^0.2.38", extras = ["all"]}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
